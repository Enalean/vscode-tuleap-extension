{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile",
            "type": "shell",
            "command": "nix-shell --run 'npm run compile' shell.nix",
            "group": "build"
        }
    ]
}
